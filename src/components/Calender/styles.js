import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  top: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    alignContent: "center",
    height: "200px",
    width: "200px",
    position: "relative",
    top: "30vh",
    left: "30vw"
  }
}));
